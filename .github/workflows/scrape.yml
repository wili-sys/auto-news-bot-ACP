name: Full News Scraper
on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: pip install beautifulsoup4 requests
      
      - name: Run full scraper
        run: python scraper.py
      
      - name: Improve news
        run: python rewrite.py
      
      - name: Commit changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add noticia_completa.txt noticia_mejorada.txt
          git commit -m "Update full news: $(date +'%Y-%m-%d %H:%M')" || echo "No changes"
          git push
